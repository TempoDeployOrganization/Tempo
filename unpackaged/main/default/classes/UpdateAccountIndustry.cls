public class UpdateAccountIndustry {
    public static void updateIndustryById(Id accountId, String newIndustry) {
        Account acc = [SELECT Id, Industry FROM Account WHERE Id = :accountId LIMIT 1];
        acc.Industry = newIndustry;
        update acc;
    }
}